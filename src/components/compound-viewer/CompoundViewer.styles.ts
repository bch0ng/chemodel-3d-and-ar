import { WebView } from 'react-native-webview';
import styled from 'styled-components';

export const JSMolWebView = styled(WebView)`
    flex: 1;
    margin: 0;
    padding: 0;
    background-color: transparent;
`;
